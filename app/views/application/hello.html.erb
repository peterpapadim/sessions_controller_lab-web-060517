<% if session[:name] %>
  <h1>Hi, <%= session[:name] %>.</h1>
  <%= form_tag ({controller: 'sessions', action: 'destroy', method: 'post'}) do %>
    <input type=submit value='logout'>
  <% end %>
<% else %>
  <%= button_to "login", sessions_new_path, method: 'get' %>
<% end %>
